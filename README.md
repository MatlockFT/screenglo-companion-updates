# SCREENGLO Companion Updates

This repository is the public, signed update channel for the SCREENGLO Windows Companion.

It contains application files only. User configuration, qBittorrent credentials, TMDB tokens, phone URLs, and local-network information are never published here. Every release manifest is signed offline; installed companions reject files with an invalid signature or checksum.
